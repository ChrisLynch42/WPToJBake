/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package com.codeexcursion.document;

import com.codeexcursion.PathCommon;
import org.junit.Assert;
import org.junit.Test;
import org.junit.Before;
import static org.junit.Assert.*;

/**
 *
 * @author lynchcs
 */
public class HTMLBuilderTest extends PathCommon {
  
/*
  @Test
  public void testGetContent() {
    HTMLBuilder builder = new HTMLBuilder(postItem);
    
    String content = builder.getContent();
    Assert.assertNotNull("Content was null!", content);
    
    String testText = "title=CodePaLOUsa Coding Contest Part 3";    
    Assert.assertTrue("Content did not begin with "+ testText, content.startsWith(testText));
    
    testText = "</html></pre>";    
    Assert.assertTrue("Content did not end with "+ testText, false);

    
    
  }*/
}
